# Copy to .env on the NAS (in /volume1/docker/portal-pinball-wikijs/), gitignored, not committed
# - same convention as Component_database and nas-dashboard's NAS-side .env files.

POSTGRES_PASSWORD=change-me-to-a-real-password
