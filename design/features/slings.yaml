id: slings
name: Sling combo
status: designed
phase: Phase 3

story:
  hook: "TBD - needs a narrative pass"

shots:
  - ref: s_left_sling
    role: left sling switch (also drives the ac_sling_left autofire coil)
  - ref: s_right_sling
    role: right sling switch (also drives the ac_sling_right autofire coil)
  - ref: led-sling-l
    role: feedback LED
  - ref: led-sling-r
    role: feedback LED

rules:
  mode: slings
  devices:
    - type: sequence
      name: sling_combo
      notes: >
        "Sling Combo" mechanic named explicitly in board Overviews.xlsx - back-to-back
        sling hits within a time window. MPF's `sequence` logic block (time-pressure,
        N hits within a timeout) is the right fit, not a plain counter.
  scoring: TBD

presentation:
  slide: TBD - no dedicated slide yet
  show: TBD
  sound: TBD

notes: ""
