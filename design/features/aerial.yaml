id: aerial
name: Aerial plate
status: designed
phase: Phase 3

story:
  hook: "TBD - needs a narrative pass"

shots:
  - ref: s-aerial
    role: aerial plate switch
  - ref: led-aerial
    role: feedback LED

rules:
  mode: aerial
  devices:
    - type: shot
      name: shot_aerial
      notes: currently just a switch with no logic - simplest feature to implement, per plans/resumption-roadmap.md Phase 3.
  scoring: TBD

presentation:
  slide: TBD - no dedicated slide yet
  show: TBD
  sound: TBD

notes: >
  board Overviews.xlsx's "Modes" sheet has a bare "aerial plate active" note and
  nothing else - the mechanic behind this shot (what "aerial" actually means for this
  machine) needs a design pass with the user before implementation.
