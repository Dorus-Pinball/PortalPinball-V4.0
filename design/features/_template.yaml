# Copy this file to design/features/<id>.yaml to start a new feature.
# See design/README.md for the full workflow this maps to, and
# design/schema/feature.schema.json for what's required vs optional.
#
# This template intentionally only fills in `id` - copy it, then work
# through the layers top to bottom. It will fail schema validation
# (tests/test_design_docs.py) until at least `name`, `status`, `shots`,
# and `rules.mode` are filled in - that's deliberate, not a bug.

id: my_new_feature
# name: Human-readable name
# status: idea

# story:
#   hook: "TBD - needs a narrative pass"
#   beats:
#     - ...

# shots:
#   - ref: s-some-switch          # must be a real name from hardware-*.yaml
#     role: primary shot switch

# rules:
#   mode: my_new_feature
#   devices:
#     - type: shot
#       name: shot_my_new_feature
#       notes: ...
#   scoring: TBD

# presentation:
#   slide: TBD - no dedicated slide yet
#   show: TBD
#   sound: TBD

# notes: ""
