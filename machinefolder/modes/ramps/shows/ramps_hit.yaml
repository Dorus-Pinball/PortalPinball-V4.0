#show_version=6
# "Glowing 'bridge extends' animation" per design/features/ramps.yaml's show note, propulsion-gel
# orange matching the slide's own accent color.
- lights:
    (led): white
  duration: 0.1s
- lights:
    (led): orange
  duration: 0.3s
